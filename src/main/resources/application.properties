# Application Name
spring.application.name=vehicle

# Server Port (Render provides PORT environment variable)
server.port=${PORT:8080}

# MongoDB Atlas Connection with TLS/SSL
spring.data.mongodb.uri=mongodb+srv://testapi0905_db_user:jgHPXKi8eZeee057@testandtry.99i2u3s.mongodb.net/park_karo_db?retryWrites=true&w=majority&tls=true&connectTimeoutMS=30000&socketTimeoutMS=30000&serverSelectionTimeoutMS=30000

# MongoDB Settings
spring.data.mongodb.auto-index-creation=true

# Actuator Settings (for Render health checks)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.endpoint.health.probes.enabled=true

# Logging
logging.level.root=WARN
logging.level.com.park_karo=INFO
logging.level.org.springframework.data.mongodb=INFO
logging.level.org.mongodb.driver=WARN