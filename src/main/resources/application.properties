spring.application.name=vehicle

# ====================================================================
# MongoDB Atlas Connection Settings (Using standard spring.data.mongodb prefix)
# ====================================================================

# 1. Connection URI (UPDATED)
# The database name 'park_karo_db' is now included in the URI path itself.
spring.data.mongodb.uri=mongodb+srv://testapi0905_db_user:mfRnItg5Rg2aOEVR@testandtry.99i2u3s.mongodb.net/park_karo_db?appName=Testandtry


# 2. Database Name (OPTIONAL, but harmless if the URI is correct)
# Using 'spring.data.mongodb.database' for consistency.
# Note: Since the database is in the URI, this line is often redundant.
# spring.mongodb.database= park_karo_db 

# ====================================================================
# Optional: Server Port (Spring Boot default is 8080)
# ====================================================================
server.port=8080