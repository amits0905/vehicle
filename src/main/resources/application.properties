# ====================================================================
# Application Name
# ====================================================================
spring.application.name=vehicle

# ====================================================================
# Server Port
# ====================================================================
server.port=8080

# ====================================================================
# MongoDB Atlas Connection Settings
# ====================================================================

# Correct MongoDB URI (Includes username, password, hosts, and target database)
spring.data.mongodb.uri=mongodb+srv://testapi0905_db_user:jgHPXKi8eZeee057@testandtry.99i2u3s.mongodb.net/park_karo_db?retryWrites=true&w=majority

# Atlas authentication DB is always admin
spring.data.mongodb.authentication-database=admin

# Optional recommended setting: Auto-create indexes on startup
spring.data.mongodb.auto-index-creation=true

logging.level.com.park_karo=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.beans=DEBUG

# ====================================================================
# Logging Configuration (To hide verbose INFO logs on startup)
# ====================================================================

# Hides "Adding discovered server..." and other cluster monitoring INFO logs
logging.level.org.mongodb.driver.cluster=WARN

# Hides the very long "MongoClient with metadata..." INFO log
logging.level.org.mongodb.driver.client=WARN

# Hides "Tomcat started on port 8080..." INFO log
#logging.level.org.springframework.boot.web.embedded.tomcat=WARN